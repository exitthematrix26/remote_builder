apiVersion: apps/v1
kind: Deployment
metadata:
  name: bb-storage
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rbe-tenant.labels" . | nindent 4 }}
    {{- include "rbe-tenant.storage.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.storage.replicas }}
  selector:
    matchLabels:
      {{- include "rbe-tenant.storage.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "rbe-tenant.storage.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap-storage.yaml") . | sha256sum }}
    spec:
      nodeSelector:
        {{- toYaml .Values.storage.nodeSelector | nindent 8 }}

      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}

      initContainers:
        - name: init-storage-dirs
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /storage/cas/persistent_state
              mkdir -p /storage/ac/persistent_state
              echo "Storage directories created:"
              find /storage -type d
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          volumeMounts:
            - name: storage
              mountPath: /storage

      containers:
        - name: bb-storage
          image: "{{ .Values.image.storage.repository }}:{{ .Values.image.storage.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["/config/storage.json"]

          ports:
            - name: grpc
              containerPort: {{ .Values.storage.port }}
              protocol: TCP

          readinessProbe:
            grpc:
              port: {{ .Values.storage.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6

          livenessProbe:
            grpc:
              port: {{ .Values.storage.port }}
            initialDelaySeconds: 30
            periodSeconds: 20

          resources:
            {{- toYaml .Values.storage.resources | nindent 12 }}

          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: storage
              mountPath: /storage

      volumes:
        - name: config
          configMap:
            name: bb-storage-config
        - name: storage
          emptyDir:
            sizeLimit: 2Gi
